<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mypro.dao.TableMapper">

    <select id="keshihuashuju" parameterType="Map" resultType="Map">
		${sql}
	</select>

    <select id="shifouchongfu" parameterType="Map" resultType="java.lang.Long">
		SELECT COUNT(*) count FROM ${table} where ${ziduan} = '${neirong}'
	</select>

    <insert id="executeadd" parameterType="Map">
		${sql}
	</insert>

    <select id="executequery" parameterType="Map" resultType="Map">
		${sql}
	</select>

    <select id="queryone" parameterType="Map" resultType="Map">
		${sql}
	</select>

</mapper>